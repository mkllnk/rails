script: 'ci/travis.rb'
before_install:
  - gem install bundler -v 1.17.2
rvm:
  - 2.2
  - 2.3
env:
  - "GEM=railties"
  - "GEM=ap,am,amo,as"
  - "GEM=ar:mysql"
  - "GEM=ar:mysql2"
  - "GEM=ar:sqlite3"
  - "GEM=ar:postgresql"
bundler_args: --path vendor/bundle --without test
services:
  - memcached
